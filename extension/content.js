// Injected by JobTrackr Auto-Apply extension.
// Listens for fill commands from the popup script (executeScript handles it directly).
// This file exists so the extension has a content script registered for permissions.
